"""
app package
-----------
Root package for the Insurance Claims REST API.

This marks the `app` directory as a Python package so that modules can be
imported using the fully-qualified path (e.g. `from app.config import settings`).
Keeping the application inside a single top-level package keeps imports
unambiguous and makes the project easy to extend into sub-packages later
(models, schemas, services, repositories, etc.).
"""
