"""
app.utils package
-----------------
Cross-cutting helpers that are not tied to any single feature (logging,
common formatters, small reusable utilities). Feature-agnostic code lives
here so it can be shared across routes, services and future modules.
"""
