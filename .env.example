# -----------------------------------------------------------------------------
# Example environment configuration.
# Copy this file to `.env` and fill in real values. NEVER commit the real .env.
#   cp .env.example .env
# -----------------------------------------------------------------------------

# Full SQLAlchemy database URL (psycopg 3 driver).
# Format: postgresql+psycopg://<user>:<password>@<host>:<port>/<database>
DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/insurance_claims

# Logging level: DEBUG | INFO | WARNING | ERROR | CRITICAL
LOG_LEVEL=INFO

# Path to the application log file (relative to project root).
LOG_FILE=logs/app.log

# --- Authentication (optional) ---
# When AUTH_ENABLED=true, data endpoints require header:  X-API-Key: <API_KEY>
# (/health and the docs stay public). Disabled by default.
AUTH_ENABLED=false
API_KEY=change-me

# --- Rate limiting (optional) ---
# When RATE_LIMIT_ENABLED=true, each client IP is limited to RATE_LIMIT
# (slowapi syntax, e.g. "100/minute"). Disabled by default.
RATE_LIMIT_ENABLED=false
RATE_LIMIT=100/minute

# --- Response caching (optional) ---
# When CACHE_ENABLED=true, report aggregates are cached in-process for
# CACHE_TTL_SECONDS and invalidated automatically after each upload.
CACHE_ENABLED=false
CACHE_TTL_SECONDS=60
